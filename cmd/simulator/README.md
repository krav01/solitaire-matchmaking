# Simulator entry point

Implemented in stage 3. The simulator will exercise rating and matching packages
with reproducible arrival streams, skill distributions and tournament partitions.
It must report fill-time distributions together with fairness and timeout rates.
This directory deliberately does not contain a fake executable or fabricated metrics.
